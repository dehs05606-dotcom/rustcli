# Tool reference

<!-- Generated by `python -m fullagent.introspect --write-docs`. Do not edit by hand: run-checks.sh fails when this file and the registry disagree. -->

17 tools. Each one's schema, permission class and error set is derived from its registry entry in
`fullagent/tools.py`, so this page cannot describe a tool differently from the way it actually behaves.

| Tool | Needs | Repeatable | Approval | Errors |
|---|---|---|---|---|
| `apply_patch` | fs.read, fs.write | non_idempotent | yes | 6 |
| `copy_path` | fs.read, fs.write | idempotent | yes | 6 |
| `create_directory` | fs.write | idempotent | no | 6 |
| `delete_path` | fs.delete | unsafe | yes | 6 |
| `edit_file` | fs.read, fs.write | non_idempotent | yes | 6 |
| `file_info` | fs.read | idempotent | no | 6 |
| `glob_files` | fs.read | idempotent | no | 6 |
| `list_dir` | fs.read | idempotent | no | 6 |
| `live_shell` | proc.exec | unsafe | yes | 5 |
| `live_shell_reset` | proc.exec | idempotent | no | 5 |
| `move_path` | fs.delete, fs.write | unsafe | yes | 6 |
| `read_file` | fs.read | idempotent | no | 6 |
| `run_command` | proc.exec | unsafe | yes | 5 |
| `search_files` | fs.read | idempotent | no | 6 |
| `web_fetch` | net.fetch | idempotent | yes | 5 |
| `web_search` | net.fetch | idempotent | yes | 5 |
| `write_file` | fs.read, fs.write | idempotent | yes | 6 |

## `apply_patch`

Apply a unified diff (git diff / diff -u format) to the working tree. Multi-file edits in one call; each hunk is context-checked and fails loudly on mismatch instead of corrupting files. Returns a per-file 'N additions / M removals' report.

| Argument | Type | Required | Meaning |
|---|---|---|---|
| `patch` | string | yes | — |

**Needs:** fs.read, fs.write · **Repeatable:** non_idempotent · **Timeout:** 60s · **Approval:** required

**Can fail with:** `E_CONFLICT`, `E_INTERNAL`, `E_NOT_FOUND`, `E_PERMISSION`, `E_RESOURCE`, `E_VALIDATION`

## `copy_path`

Copy a file or directory.

| Argument | Type | Required | Meaning |
|---|---|---|---|
| `dst` | string | yes | — |
| `src` | string | yes | — |

**Needs:** fs.read, fs.write · **Repeatable:** idempotent · **Timeout:** 60s · **Approval:** required

**Can fail with:** `E_CONFLICT`, `E_INTERNAL`, `E_NOT_FOUND`, `E_PERMISSION`, `E_RESOURCE`, `E_VALIDATION`

## `create_directory`

Create a directory (parents included).

| Argument | Type | Required | Meaning |
|---|---|---|---|
| `path` | string | yes | — |

**Needs:** fs.write · **Repeatable:** idempotent · **Timeout:** 60s · **Approval:** not required

**Can fail with:** `E_CONFLICT`, `E_INTERNAL`, `E_NOT_FOUND`, `E_PERMISSION`, `E_RESOURCE`, `E_VALIDATION`

## `delete_path`

Delete a file or directory permanently.

| Argument | Type | Required | Meaning |
|---|---|---|---|
| `path` | string | yes | — |

**Needs:** fs.delete · **Repeatable:** unsafe · **Timeout:** 60s · **Approval:** required

**Can fail with:** `E_CONFLICT`, `E_INTERNAL`, `E_NOT_FOUND`, `E_PERMISSION`, `E_RESOURCE`, `E_VALIDATION`

## `edit_file`

Replace an exact string in a file. old_string must match exactly (including indentation) and uniquely, unless replace_all=true.

| Argument | Type | Required | Meaning |
|---|---|---|---|
| `new_string` | string | yes | — |
| `old_string` | string | yes | — |
| `path` | string | yes | — |
| `replace_all` | boolean | no | — |

**Needs:** fs.read, fs.write · **Repeatable:** non_idempotent · **Timeout:** 30s · **Approval:** required

**Can fail with:** `E_CONFLICT`, `E_INTERNAL`, `E_NOT_FOUND`, `E_PERMISSION`, `E_RESOURCE`, `E_VALIDATION`

## `file_info`

Show metadata (size, mtime, type) for a path.

| Argument | Type | Required | Meaning |
|---|---|---|---|
| `path` | string | yes | — |

**Needs:** fs.read · **Repeatable:** idempotent · **Timeout:** 20s · **Approval:** not required

**Can fail with:** `E_CONFLICT`, `E_INTERNAL`, `E_NOT_FOUND`, `E_PERMISSION`, `E_RESOURCE`, `E_VALIDATION`

## `glob_files`

Find files by glob pattern, e.g. '**/*.py'.

| Argument | Type | Required | Meaning |
|---|---|---|---|
| `path` | string | no | — |
| `pattern` | string | yes | — |

**Needs:** fs.read · **Repeatable:** idempotent · **Timeout:** 30s · **Approval:** not required

**Can fail with:** `E_CONFLICT`, `E_INTERNAL`, `E_NOT_FOUND`, `E_PERMISSION`, `E_RESOURCE`, `E_VALIDATION`

## `list_dir`

List a directory's contents (one level).

| Argument | Type | Required | Meaning |
|---|---|---|---|
| `path` | string | no | — |

**Needs:** fs.read · **Repeatable:** idempotent · **Timeout:** 20s · **Approval:** not required

**Can fail with:** `E_CONFLICT`, `E_INTERNAL`, `E_NOT_FOUND`, `E_PERMISSION`, `E_RESOURCE`, `E_VALIDATION`

## `live_shell`

Run a command in a PERSISTENT bash session: cd, exports and background jobs survive between calls. Use for live workflows (cd src && build, then run tests, then inspect, then fix).

| Argument | Type | Required | Meaning |
|---|---|---|---|
| `command` | string | yes | — |
| `timeout` | integer | no | seconds, default 120 |

**Needs:** proc.exec · **Repeatable:** unsafe · **Timeout:** 300s · **Approval:** required

**Can fail with:** `E_INTERNAL`, `E_PERMISSION`, `E_TIMEOUT`, `E_UPSTREAM`, `E_VALIDATION`

## `live_shell_reset`

Reset the persistent live-shell session back to the process working directory.

Takes no arguments.

**Needs:** proc.exec · **Repeatable:** idempotent · **Timeout:** 60s · **Approval:** not required

**Can fail with:** `E_INTERNAL`, `E_PERMISSION`, `E_TIMEOUT`, `E_UPSTREAM`, `E_VALIDATION`

## `move_path`

Move/rename a file or directory.

| Argument | Type | Required | Meaning |
|---|---|---|---|
| `dst` | string | yes | — |
| `src` | string | yes | — |

**Needs:** fs.delete, fs.write · **Repeatable:** unsafe · **Timeout:** 60s · **Approval:** required

**Can fail with:** `E_CONFLICT`, `E_INTERNAL`, `E_NOT_FOUND`, `E_PERMISSION`, `E_RESOURCE`, `E_VALIDATION`

## `read_file`

Read a text file with line numbers. Use offset/limit for large files.

| Argument | Type | Required | Meaning |
|---|---|---|---|
| `limit` | integer | no | max lines (default 1000) |
| `offset` | integer | no | first line (1-based) |
| `path` | string | yes | — |

**Needs:** fs.read · **Repeatable:** idempotent · **Timeout:** 20s · **Approval:** not required

**Can fail with:** `E_CONFLICT`, `E_INTERNAL`, `E_NOT_FOUND`, `E_PERMISSION`, `E_RESOURCE`, `E_VALIDATION`

## `run_command`

Run a shell command via bash and return exit code, stdout, stderr. Use for builds, tests, git, installs, running programs.

| Argument | Type | Required | Meaning |
|---|---|---|---|
| `command` | string | yes | — |
| `timeout` | integer | no | seconds, default 120 |

**Needs:** proc.exec · **Repeatable:** unsafe · **Timeout:** 300s · **Approval:** required

**Can fail with:** `E_INTERNAL`, `E_PERMISSION`, `E_TIMEOUT`, `E_UPSTREAM`, `E_VALIDATION`

## `search_files`

Regex search through file contents (ripgrep-style). Returns path:line:content for matches.

| Argument | Type | Required | Meaning |
|---|---|---|---|
| `glob_filter` | string | no | filename glob, e.g. '*.py' |
| `path` | string | no | dir or file to search |
| `pattern` | string | yes | regex pattern |

**Needs:** fs.read · **Repeatable:** idempotent · **Timeout:** 60s · **Approval:** not required

**Can fail with:** `E_CONFLICT`, `E_INTERNAL`, `E_NOT_FOUND`, `E_PERMISSION`, `E_RESOURCE`, `E_VALIDATION`

## `web_fetch`

Fetch a URL and return its text content.

| Argument | Type | Required | Meaning |
|---|---|---|---|
| `url` | string | yes | — |

**Needs:** net.fetch · **Repeatable:** idempotent · **Timeout:** 30s · **Approval:** required · **Retry:** up to 3 attempts on E_TIMEOUT, E_UPSTREAM

**Can fail with:** `E_INTERNAL`, `E_PERMISSION`, `E_TIMEOUT`, `E_UPSTREAM`, `E_VALIDATION`

## `web_search`

Search the web (DuckDuckGo) and return top results.

| Argument | Type | Required | Meaning |
|---|---|---|---|
| `query` | string | yes | — |

**Needs:** net.fetch · **Repeatable:** idempotent · **Timeout:** 45s · **Approval:** required · **Retry:** up to 3 attempts on E_TIMEOUT, E_UPSTREAM

**Can fail with:** `E_INTERNAL`, `E_PERMISSION`, `E_TIMEOUT`, `E_UPSTREAM`, `E_VALIDATION`

## `write_file`

Create or overwrite a file with the given content. Parent dirs are created.

| Argument | Type | Required | Meaning |
|---|---|---|---|
| `content` | string | yes | — |
| `path` | string | yes | — |

**Needs:** fs.read, fs.write · **Repeatable:** idempotent · **Timeout:** 30s · **Approval:** required

**Can fail with:** `E_CONFLICT`, `E_INTERNAL`, `E_NOT_FOUND`, `E_PERMISSION`, `E_RESOURCE`, `E_VALIDATION`
